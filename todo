Solitare Todo list:

Add Drag + Drop feature
Add "Top Card / isDraggable" property to cards in the deck / pile
Add logic to where you can drop the card. 
    Stacks -> if empty, must be an ace else, must be the next card in order
    Piles --> mu




